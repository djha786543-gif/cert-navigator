# Career Navigator v1 — Railway deployment deps (SQLite stack only)
# Heavy deps (sentence-transformers, asyncpg, celery, pgvector) excluded.

# Web framework
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0

# Database (SQLite via sync SQLAlchemy)
sqlalchemy>=2.0.36

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.2.0
python-multipart>=0.0.12

# Validation
pydantic[email]>=2.10.0

# HTTP client (Adzuna job API)
httpx>=0.28.0

# Background scheduling (daily job refresh)
APScheduler>=3.10.4

# Resume parsing
pdfplumber>=0.11.0

# Claude API (Study Vault artifact generation)
anthropic>=0.40.0
